export const environment = {
    // Indica si la aplicación está en modo de producción o no.
    production: false,
    // Define la URL del servidor de sockets (Socket.IO) que se utilizará para establecer una conexión WebSocket.
    socketUrl: 'http://192.168.2.68:3000/'
}